services:
  - type: worker
    name: eslatma-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python eslatma_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
